const LayoutHeader = () => (
  // TODO: Should be replaced with a leva panel
  <header className='fixed top-0 w-screen flex align-center justify-between p-10 z-20 pointer-events-auto'>
    <div id='layout-header-left'></div>
    <div id='layout-header-right'></div>
  </header>
)

export default LayoutHeader
